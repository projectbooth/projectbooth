# nfl-betting

Weekly cadence, a trained model, a "what should I look at" UI — ARCHITECTURE.md §9. Reuses
feature/elo functions meant to live in `platform-sdk`, not reimplemented here. Needs Serving
(Phase 6, for MLflow) before the model-tracking half works end to end.
