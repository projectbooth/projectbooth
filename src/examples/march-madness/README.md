# march-madness

Bursty, deadline-driven, needs a fast turnaround from Kaggle dataset to submission —
ARCHITECTURE.md §9. Reuses `nfl-betting`'s feature functions and existing model from the registry
rather than reinventing them — that reuse is the whole point of building this platform once.
