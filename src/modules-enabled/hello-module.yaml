# GENERATED by `platform module install hello-module` at 2026-09-11T02:13:20Z — do not hand-edit.
# Source descriptor: src/modules/hello-module/module.yaml. To change this module's placement,
# namespace, or chart, edit that file and re-run `platform module install hello-module`
# (it's safe to run again — it overwrites this file in place and commits the diff).
#
# Picked up by modules-root (src/core/argocd/apps/core/modules-root.yaml), which watches
# src/modules-enabled/ the same way root-app.yaml watches apps/core/ — see that file's header
# comment for the three-level app-of-apps structure. `syncPolicy.automated.prune: true` plus the
# finalizer below mean `platform module uninstall hello-module` (which just deletes this file)
# is enough to tear the whole module back down — except any PersistentVolumeClaim the chart marks
# `argocd.argoproj.io/sync-options: Delete=false`, which survives on purpose (ARCHITECTURE.md §3;
# see src/charts/hello-module/templates/*.yaml for this module's own PVCs, if it has any).
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: hello-module
  namespace: argocd
  labels:
    platform.io/tier: module
  annotations:
    platform.io/display-name: "Hello Module"
    platform.io/icon: "puzzle"
    platform.io/nav-path: "/hello-module"
    platform.io/proxy-to: "http://hello-module.hello-module.svc:80"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: git@github.com:DougallPercival/opendataplatform.git
    targetRevision: dev
    path: src/charts/hello-module
    helm:
      values: |
        placement:
          role: compute
          tolerations:
            - key: platform.io/role
              operator: Equal
              value: compute
              effect: NoSchedule
  destination:
    server: https://kubernetes.default.svc
    namespace: hello-module
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
