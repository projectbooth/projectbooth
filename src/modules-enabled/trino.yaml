# GENERATED by `platform module install trino` at 2026-09-15T04:26:42Z — do not hand-edit.
# Source descriptor: src/modules/trino/module.yaml. To change this module's placement,
# namespace, or chart, edit that file and re-run `platform module install trino`
# (it's safe to run again — it overwrites this file in place and commits the diff).
#
# Picked up by modules-root (src/core/argocd/apps/core/modules-root.yaml), which watches
# src/modules-enabled/ the same way root-app.yaml watches apps/core/ — see that file's header
# comment for the three-level app-of-apps structure. `syncPolicy.automated.prune: true` plus the
# finalizer below mean `platform module uninstall trino` (which just deletes this file)
# is enough to tear the whole module back down — except any PersistentVolumeClaim the chart marks
# `argocd.argoproj.io/sync-options: Delete=false`, which survives on purpose (ARCHITECTURE.md §3;
# Chart: trino 1.42.2 from https://trinodb.github.io/charts (not this repo — see module.yaml's own externalChart).
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: trino
  namespace: argocd
  labels:
    platform.io/tier: module
  annotations:
    platform.io/display-name: "Trino"
    platform.io/icon: "puzzle"
    platform.io/nav-path: "/trino"
    platform.io/proxy-to: "http://trino-coordinator.trino.svc:8080"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://trinodb.github.io/charts
    chart: trino
    targetRevision: 1.42.2
    helm:
      values: |
        placement:
          role: compute
          tolerations:
          - key: platform.io/role
            operator: Equal
            value: compute
            effect: NoSchedule
        catalogs:
          iceberg: 'connector.name=iceberg

            iceberg.catalog.type=jdbc

            iceberg.jdbc-catalog.connection-url=jdbc:postgresql://platform-postgres-rw.postgres.svc:5432/trino_catalog

            iceberg.jdbc-catalog.connection-user=trino

            iceberg.jdbc-catalog.connection-password=${ENV:TRINO_DB_PASSWORD}

            fs.native-s3.enabled=true

            s3.endpoint=http://storage-seaweedfs-s3.storage-seaweedfs.svc:8333

            s3.path-style-access=true

            s3.aws-access-key=${ENV:TRINO_S3_ACCESS_KEY}

            s3.aws-secret-key=${ENV:TRINO_S3_SECRET_KEY}

            '
        env:
        - name: TRINO_DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: platform-postgres-trino-credentials
              key: password
        - name: TRINO_S3_ACCESS_KEY
          valueFrom:
            secretKeyRef:
              name: platform-s3-credentials
              key: ACCESS_KEY_ID
        - name: TRINO_S3_SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: platform-s3-credentials
              key: ACCESS_SECRET_KEY
  destination:
    server: https://kubernetes.default.svc
    namespace: trino
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
