# Copy to .env for local dev — see platform_sdk/config.py's docstring for
# why these are env vars, not a config file, and why PLATFORM_USER/
# PLATFORM_ROLE default to unset rather than a hardcoded value.

PLATFORM_CATALOG_URL=http://localhost:8000
PLATFORM_WORKSPACE=personal
# PLATFORM_USER=alice
# PLATFORM_ROLE=viewer
